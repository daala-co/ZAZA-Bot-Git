# ... (tout le reste de ton code précédent ici, inchangé)
# Exemple uniquement de la ligne corrigée :
return f"*{token_name}* ({symbol}) -> RSI {rsi:.2f} | {rsi_status} | {macd_status} | {trend_status} | {price_info} | {action}"
# ... (le reste de ton fichier continue ici)